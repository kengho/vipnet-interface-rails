(function() {
  vipnetInterface.remoteStatus.renderStatus({
    parentId: "<%= @response[:parent_id] %>",
    status: "true",
    tooltipText: "<%= @response[:tooltip_text] %>",
    undo: "true",
    html: "<%= j render 'nodes/row/remote_status/button_undo' %>",
    ids: ["<%= @response[:parent_id] %> div[name='info']"],
  });
  vipnetInterface.remoteStatus.renderInfo({
    parentId: "<%= @response[:parent_id] %>",
    rowId: "<%= @response[:row_id] %>",
    html: "<%= j render 'nodes/row/info_block', node: @response[:node] %>",
  });
})();
