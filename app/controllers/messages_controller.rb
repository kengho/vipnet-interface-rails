class MessagesController < ApplicationController
  def index

# p User.all
# user = User.find(1)
# p user.valid_password?("Admin778")
# user.methods.sort.each { |m| p m }
# user = User.new(  email: "test4@test.ru",password: "Admin777", password_confirmation: "Admin777")
# p user.save
# p user
    # session = UserSession.create(:email => "admin@admin.com", :password => "Admin777")
    # session.save
    # p session
    # session2 = UserSession.find
    # session2 = UserSession.find(:email => "admin@admin.com")
# UserSession.methods.sort.each { |m| p m }
# p session2
    # @messages = Message.all.paginate(page: params[:page], per_page: 50)
  end
end
